import gameEngine.Event;
import gameEngine.EventManager;
import gameEngine.LevelManager;

void initialize()
{
}


void handleEvent(Event event)
{
   if (event.getEventId() == "eventKP_SPACE")
   {
      LevelManager.advanceLevel("levelKitchenCounter");
   }   
}